services:
  mongo:
    image: mongo:7
    container_name: wdp301_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: wdp301_historytalk

volumes:
  mongo_data:
